export interface Experience {
  title: string;
  company: string;
  period: string;
  technologies: string[];
  description: string;
}

export interface AboutData {
  title: string;
  intro: string;
  profileImageId: string;
}
